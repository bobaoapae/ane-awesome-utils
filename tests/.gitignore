# Test harness outputs — generated by run_test.ps1 / build.bat.
# Sources live in app/ and shared/profiler/*/, outputs under output/.

profiler-e2e/output/
profiler-e2e/app_stdout.log
profiler-e2e/app_stderr.log

# rva-probe: the initial RVA probe captured logs + bytes. Scripts are
# kept (probe.cdb, run_probe.ps1, tcp_listener.py); any captured data is
# ephemeral.
rva-probe/listener.log
rva-probe/listener.log.err
rva-probe/probe_*.log
rva-probe/probe.log
rva-probe/scout_bytes.bin
